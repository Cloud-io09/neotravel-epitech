import type { Lead } from "@/shared/types/lead";

// Base vierge : aucune donnée de démo. Les demandes réelles arrivent via le
// formulaire d'inscription (ou Supabase une fois branché).
export const mockLeads: Lead[] = [];
